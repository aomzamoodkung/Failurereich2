focus_tree = {
	id = hashemite_focus
	country = {
		factor = 0
		modifier = {
			add = 10
			tag = HEJ
		}
	}
	focus = {
		id = HEJ_invade_saudi
		text="Invade The Saudi State"
		icon = GFX_goal_generic_military_sphere
		x = 1
		y = 0
		cost = 5
		available_if_capitulated = yes
		
		bypass = {
			   NOT = { country_exists = SAU }
		}

		ai_will_do = {
			factor = 1
		}
		completion_reward = {
			add_stability = 0.15 
			add_political_power = 100

	        	create_wargoal = {
		                target = SAU
		                type = annex_everything
	        	}
	
			SAU = {
				every_owned_state = {
				    add_claim_by = HEJ
				}
			}
		}
	}

	focus = {
		id = HEJ_take_hail
		text="Invade Hail region"
		icon = GFX_goal_generic_military_sphere
		x = 3
		y = 0
		cost = 5
		available_if_capitulated = yes
		ai_will_do = {
			factor = 1
		}
		
		bypass = {
			   NOT = { country_exists = JSH }
		}

		completion_reward = {
			add_stability = 0.15 
			add_political_power = 100

	        	create_wargoal = {
		                target = JSH
		                type = annex_everything
	        	}	
			JSH = {
				every_owned_state = {
				    add_claim_by = SAU
				}
			}
		}
	}

	focus = {
		id = HEJ_invade_uae
		text="Invade Hassa"
		icon = GFX_goal_generic_military_sphere
		x = 5
		y = 0
		cost = 5
		available_if_capitulated = yes
		ai_will_do = {
			factor = 1
		}
		
		bypass = {
			   NOT = { country_exists = UAE }
		}

		completion_reward = {
			add_stability = 0.15 
			add_political_power = 100

	        	create_wargoal = {
		                target = UAE
		                type = annex_everything
	        	}

			UAE = {
				every_owned_state = {
				    add_claim_by = HEJ
				}
			}	
		}
	}

	focus = {
		id = HEJ_invade_oman
		text="Invade Oman"
		icon = GFX_goal_generic_military_sphere
		prerequisite = { focus = HEJ_invade_saudi }
		prerequisite = { focus = HEJ_take_hail }
		prerequisite = { focus = HEJ_invade_uae }
		x = 2
		y = 1
		cost = 5
		available_if_capitulated = yes
		ai_will_do = {
			factor = 1
		}
		available = {
			   NOT = { country_exists = SAU }
			   NOT = { country_exists = UAE }
			   NOT = { country_exists = JSH }
		}
		
		bypass = {
			   NOT = { country_exists = OMA }
		}

		completion_reward = {

	        	create_wargoal = {
		                target = OMA
		                type = annex_everything
	        	}

			OMA = {
				every_owned_state = {
				    add_claim_by = HEJ
				}
			}	
		}
	}

	focus = {
		id = HEJ_invade_yemen
		text="Invade Yemen"
		icon = GFX_goal_generic_military_sphere
		prerequisite = { focus = HEJ_invade_saudi }
		prerequisite = { focus = HEJ_take_hail }
		prerequisite = { focus = HEJ_invade_uae }
		x = 4
		y = 2
		cost = 5
		available_if_capitulated = yes
		ai_will_do = {
			factor = 1
		}
		available = {
			   NOT = { country_exists = SAU }
			   NOT = { country_exists = UAE }
			   NOT = { country_exists = JSH }
		}
			
		bypass = {
			   NOT = { country_exists = YEM }
		}

		completion_reward = {

	        	create_wargoal = {
		                target = YEM
		                type = annex_everything
	        	}

			YEM = {
				every_owned_state = {
				    add_claim_by = HEJ
				}
			}	
		}
	}

	focus = {
		id = HEJ_arabia
		text="Create Arab Federation"
		icon = GFX_goal_generic_military_sphere
		prerequisite = { focus = HEJ_invade_yemen }
		prerequisite = { focus = HEJ_invade_oman }
		x = 3
		y = 3
		cost = 5
		available_if_capitulated = yes

		available = {
			owns_state = 679
			owns_state = 857
			owns_state = 859
			owns_state = 292
		}

		ai_will_do = {
			factor = 1
		}

		completion_reward = {
	        	set_cosmetic_tag = HEJ_arabia
				
				SAU = {
					every_core_state = {
						add_core_of = HEJ
					}
				}
				UAE = {
					every_core_state = {
						add_core_of = HEJ
					}
				}
				YEM = {
					every_core_state = {
						add_core_of = HEJ
					}
				}
				OMA = {
					every_core_state = {
						add_core_of = HEJ
					}
				}
		}
	}

#### Beyond the Gulf states ####

	focus = {
		id = HEJ_invite_ARA
		text="ask Hashemite friends"
		icon = GFX_goal_generic_military_sphere
		prerequisite = { focus = HEJ_arabia }
		x = 2
		y = 4
		cost = 5
		available_if_capitulated = yes
		ai_will_do = {
			factor = 1
		}
		
		bypass = {
			   NOT = { country_exists = JOR }
		}

		completion_reward = {
			HEJ = { days = 1 country_event = arabia.1 }
		}
	}

	focus = {
		id = HEJ_invade_Mesopotamia
		text="ask Hashemite friends"
		icon = GFX_goal_generic_military_sphere
		prerequisite = { focus = HEJ_arabia }
		x = 2
		y = 4
		cost = 5
		available_if_capitulated = yes
		ai_will_do = {
			factor = 1
		}
		
		bypass = {
			   NOT = { country_exists = SYR }
			   NOT = { country_exists = LEB }
		}

		completion_reward = {
			
	        	create_wargoal = {
		                target = SYR
		                type = annex_everything
	        	}

			SYR = {
				every_owned_state = {
				    add_claim_by = HEJ
				}
			}	
	        	create_wargoal = {
		                target = IRQ
		                type = annex_everything
	        	}

			IRQ = {
				every_owned_state = {
				    add_claim_by = HEJ
				}
			}	
		}
	}

	focus = {
		id = HEJ_take_KUR
		text="Invade Kurdistan"
		icon = GFX_goal_generic_military_sphere
		prerequisite = { focus = HEJ_invite_ARA }
		x = 4
		y = 4
		cost = 5
		available_if_capitulated = yes
		ai_will_do = {
			factor = 1
		}

		completion_reward = {
				
	        	create_wargoal = {
		                target = KUR
		                type = annex_everything
	        	}

			KUR = {
				every_owned_state = {
				    add_claim_by = HEJ
				}
			}	
		}
	}

#### Arabian Entente ####

	focus = {
		id = HEJ_join_entente
		text="Join The Entente"
		icon = GFX_goal_generic_military_sphere
		prerequisite = { focus = HEJ_take_KUR }
		prerequisite = { focus = HEJ_invade_Mesopotamia }
		x = 3
		y = 5
		cost = 5
		available_if_capitulated = yes
		ai_will_do = {
			factor = 1
		}

		completion_reward = {
	        	CAN = { add_to_faction = HEJ }
		}
	}

	focus = {
		id = HEJ_invade_egypt
		text="Invade Egypt"
		icon = GFX_goal_generic_military_sphere
		prerequisite = { focus = HEJ_join_entente }
		x = 3
		y = 6
		cost = 5
		available_if_capitulated = yes
		ai_will_do = {
			factor = 1
		}

		completion_reward = {
				
	        	create_wargoal = {
		                target = EGY
		                type = annex_everything
	        	}

			EGY = {
				every_owned_state = {
				    add_claim_by = HEJ
				}
			}
		}
	}
 }
